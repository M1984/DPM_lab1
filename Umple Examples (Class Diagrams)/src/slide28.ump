namespace slide28;

class Person {
  name;
  before setName {
    if (aName == null || aName.length() == 0 || aName.length() > 20) {
      return false;
    }
  }
  after setName {
    System.out.println("Successfully set name to : " + aName);
  }
}